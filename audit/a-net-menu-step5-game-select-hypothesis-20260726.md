# a-net `menu`@step5 ↔ game-select scout — hypothesis only

**Date:** 2026-07-26 · **Seat:** impl-aiclient-cursor · **Status:** DRAFT / hub GO needed  
**Not a classify change.** No new screen class invented here.

## Link

| Surface | Tip / artifact | What it says |
|---|---|---|
| Live ensure matrix | `audit/live-ensure-matrix-20260726.md` | a-net NEW durable FAIL: `menu`@login step5 (redacted host). First `empty_response` withdrawn. |
| Game-select classify scout | origin `0335613` · `WO-GAME-SELECT-CLASSIFY-SCOUT` | Independent re-run of the settled-frame scout (docs + scout script only). |

## Hypothesis (not proven)

The a-net stall labelled `menu` at login step5 may be the same family the game-select scout was probing: a TWGS selection / boxed-menu frame that today's classifier (or the ensure step's expectation) treats as ordinary `menu`, so the automaton waits for a later login shape that never arrives.

This is **correlation across two redacted observations**, not a live re-classify claim. Distinguishing:

1. remote login stall (host/game actually stuck), vs
2. game-select ↔ `menu` mislabel on a settled frame,

needs a deliberate classify/ensure GO — not this note.

## Out of bounds

- Do **not** invent a new classify label from this note.
- Do **not** treat scout tip `0335613` as product proof that a-net is misclassified.
- Hostnames / credentials stay in the matrix's redaction rules; this note names neither.
