#!/bin/sh
''''exec "$(dirname "$0")"/.venv/bin/python3 "$0" "$@" # '''
"""twd — session daemon entry point. Normally spawned by `tw start`."""
from twclient.daemon import main

if __name__ == "__main__":
    main()
