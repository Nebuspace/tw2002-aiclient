# WO-P4-056 — Attach from cockpit (M takes Human lock)

> Reconstructed from coord HANDOFF bodies (2026-07-25 backfill).
> Status: **EXECUTED / DONE** 2026-07-25 · tip **`2c2decc`** (CC · Opus)
> Type: verify/build · Phase: 4 · Seat: impl-claudecode-aiclient
> Refs: `WO-P4-050-057-viewport-PREP.md` §PWO-056 · `canon/architecture/control-and-escalation.md`

## Goal
`M` hotkey takes Human lock via `AttachInputConn`; chip flip SPECTATE→`MANUAL — YOU HAVE CONTROL`; keystrokes through redaction; scope-checked single-site allowlist on 055 tripwire. Cipher: alias/`getattr` holes in no-send tripwire closed (one provable bypass class per honest residual disclosure). Esc≠detach; backspace forwarded; `q` is game input while attached.

## Scope
- `tw2002_aiclient/app.py` `_attempt_attach` / `_run_play` attach loop
- `tw2002_aiclient/screens.py` `M` intent
- `tw2002_aiclient/cockpit/control_seat.py` `attached_label`
- `tests/test_cockpit_attach.py` · `tests/test_spectate_no_send.py` allowlist

## Constraints
- Do not bypass control_lock; sender-tag hardcoded "human" daemon-side
- Secrets doctrine: password never on attach channel; redaction sink wired
- Esc ≠ detach (Esc→launcher unchanged)
- `q` is game input while attached, not quit

## Accept
1. `M` takes Human lock; chip → `MANUAL — YOU HAVE CONTROL`
2. Keystrokes forwarded with secret redaction
3. Cipher alias/`getattr` tripwire holes closed; honest residual (fully-dynamic reflection) disclosed
4. Esc→launcher unchanged; `q` reaches game

## Proof
FakeClient control_lock transitions; redaction sentinel; Esc/detach interaction owned by 057.

## Refs
`WO-P4-050-057-viewport-PREP.md` §PWO-056 · hub Accept + Push GO @ 03:38:50Z
