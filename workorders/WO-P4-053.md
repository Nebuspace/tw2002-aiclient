# WO-P4-053 — Viewport color parity (per-cell fg/bg/bold from terminal.color_map)

> Reconstructed from coord HANDOFF bodies (2026-07-25 backfill).
> Status: **EXECUTED / DONE** 2026-07-25 · tip **`eb59274`** (CC · Opus)
> Type: harden · Phase: 4 · Seat: impl-claudecode-aiclient
> Refs: `WO-P4-050-057-viewport-PREP.md` §PWO-053 · `canon/surfaces/visual-language.md` server CP437 palette

## Goal
One-lock `render_with_color` on bare `build_response`; `draw_runs` for per-cell fg/bg/bold; process-global `_SharedPairs (fg,bg)`; pair exhaustion → A_NORMAL. Per-cell colour from `terminal.color_map()` matches tip GAME viewport / visual-language "server CP437 palette" (ops `./tw spectate` RETIRED — not an Accept parity path).

## Scope
- `tw2002_aiclient/session/terminal.py` — `render_with_color` + `color_map`
- `tw2002_aiclient/cockpit/draw.py` — `draw_runs` + `_SharedPairs`
- `tests/` — Layer-A color_map fixture → attr pairs; Layer-B pty cell `.fg`/`.bold`

## Constraints
- Semantic 7-tone table is **chrome only** — never recolor game cells with `ok`/`danger`
- Pair exhaustion must degrade without crash
- One-lock `render_with_color` — not separate `raw_display()` + `color_map()` zip (different bboxes)

## Accept
1. Per-cell fg/bg/bold from `terminal.color_map()` matches visual-language "server CP437 palette"
2. Pair exhaustion degrades to A_NORMAL without crash
3. Semantic chrome tones never applied to game cells

## Proof
Layer-A — color_map fixture → attr pairs.  
Layer-B — pty cell `.fg` / `.bold` vs fixture (not ANSI-regex).

## Refs
`WO-P4-050-057-viewport-PREP.md` §PWO-053 · `visual-language.md` server CP437 · hub Accept + Push GO @ 00:18:19Z (2026-07-25)
