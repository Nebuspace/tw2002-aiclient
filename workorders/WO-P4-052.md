# WO-P4-052 — Viewport render grid (mono glyph paint)

> Reconstructed from coord HANDOFF bodies (2026-07-25 backfill).
> Status: **EXECUTED / DONE** 2026-07-24 · tip **`de47a26`** (CC · Opus)
> Type: build · Phase: 4 · Seat: impl-claudecode-aiclient
> Refs: `WO-P4-050-057-viewport-PREP.md` §PWO-052 · `canon/surfaces/trainer-cockpit.md`

## Goal
Paint pyte (or settle snapshot grid) cells into the 80×25 GAME interior via WatchFeed snapshot; per-cell mono glyph paint; top-drop under height pressure; per-cell color deferred to 053. CSI/OSC in cells must hit draw choke.

## Scope
- `tw2002_aiclient/screens.py` — GAME interior paint path from `WatchFeed`/`WatchFeedSnapshot`
- `tw2002_aiclient/cockpit/draw.py` — `draw_runs` choke
- `tests/` — Layer-A fixture grid → exact drawn lines; Layer-B pty `_find_text` for known fixture glyph

## Constraints
- Do not reinterpret server palette here (that's 053)
- CSI/OSC in cells must hit `_safe_write` draw choke
- No per-frame `newwin` / full `erase()` of static chrome

## Accept
1. pyte/settle snapshot cells drawn into GAME interior; updates on settle-edge (050)
2. ASCII/unicode glyph twin policy honored where chrome meets game edge
3. CSI/OSC/hostile content hits draw choke; no crash

## Proof
Layer-A — fixture grid → exact drawn lines (clip/sanitize via `cockpit/draw`).  
Layer-B — pty `_find_text` for known fixture glyph/string.

## Refs
`WO-P4-050-057-viewport-PREP.md` §PWO-052 · hub Accept + Push GO @ 23:44:31Z
