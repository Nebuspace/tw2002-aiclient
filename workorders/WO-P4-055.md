# WO-P4-055 — Product spectate mode (in-cockpit read-only observation)

> Reconstructed from coord HANDOFF bodies (2026-07-25 backfill).
> Status: **EXECUTED / DONE** 2026-07-25 · tip **`37b3e99`** (CC · Opus)
> Type: build · Phase: 4 · Seat: impl-claudecode-aiclient
> Refs: `WO-P4-050-057-viewport-PREP.md` §PWO-055 · `canon/surfaces/spectate-and-attach.md`

## Goal
In-cockpit `PlayShellScreen.spectating` default `True` + pure `cockpit/control_seat.py` muted `SPECTATE` chip (liveness-priority collision guard). AST no-send tripwire `tests/test_spectate_no_send.py` (method-name `send_request`, detector meta-test, whole-`app.py` guard) established for 056 allowlist. Does NOT ship ops `tw spectate` CLI (F2 HOLD).

## Scope
- `tw2002_aiclient/cockpit/control_seat.py` — `SPECTATE` chip / muted label
- `tw2002_aiclient/screens.py` `PlayShellScreen.spectating`
- `tw2002_aiclient/app.py`
- `tests/test_cockpit_spectate.py` · `tests/test_spectate_no_send.py`

## Constraints
- **F2 HOLD** — ops `tw spectate` CLI remains Max-gated; stay cockpit-only
- No sends from spectate mode (no-send tripwire enforced)
- User-facing leave = attach (056)

## Accept
1. Cockpit enters read-only spectate mode (watch without lock); **no sends**
2. Muted `SPECTATE` badge per visual-language
3. **Does not** ship `tw spectate` ops CLI
4. AST no-send tripwire green

## Proof
Static assert no send path; FakeClient subscribe-only; TTY mode indicator honest muted/`—`.

## Refs
`WO-P4-050-057-viewport-PREP.md` §PWO-055 · hub Accept + Push GO @ 02:34:58Z
