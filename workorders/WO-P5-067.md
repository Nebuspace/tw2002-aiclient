# WO-P5-067 — `R` Record macro wire

**Status:** OPEN · READY  
**Posted:** 2026-07-27T05:55:00Z · vision refill after extreme Play prove Accept  
**Seat:** `impl-claudecode-aiclient` (Fable) — CERT still parked; this is the next product lane  
**Depends:** tip ≥ `9028920` · teach strip labels visible (066 live on Play — proven extreme audit)  
**Refs:** `WO-P5-060-072-mode-teach-PREP.md` §PWO-067 · `canon/surfaces/mode-line-and-teach-controls.md`

## Goal

Wire `R` Record: human-initiated record captures keystrokes to a macro store/ledger. No live AI drive. Secrets redacted on the capture path.

## Scope

- Record start/stop intent from Play teach strip `R`
- Persist captured keystrokes (file and/or ledger artifact per existing patterns)
- Tests: FakeClient record start/stop · redaction on secret keystrokes · no autopilot fire from record alone

## Constraints

- Do not bind `R` to attach/launch.
- Secrets doctrine — never log passwords / `secret=True` sends as plaintext.
- Do not implement full Phase-6 rule engine.
- Stay off CERT-JUNIT until hub lifts that STOP.

## Accept

1. Explicit `R` starts recording; second `R` (or documented stop) ends it.
2. Artifact exists with captured non-secret keys; secret sends redacted.
3. Record path never calls explore/ensure auto-arm.
4. PR + STATUS.

## Proof

Unit/FakeClient + greps; optional pty. No live TWGS required for Accept (live optional).
