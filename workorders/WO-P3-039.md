# WO-P3-039 — Responsive fold (GOALS+FOCUS stacked into DECISIONS below 138)

> Reconstructed from coord HANDOFF bodies (2026-07-25 backfill).
> Status: **EXECUTED / DONE** 2026-07-24 · tip **`2841e9f`** (CC · Fable 5)
> Type: build · Phase: 3 · Seat: impl-claudecode-aiclient
> Refs: `WO-P3-034-041-panels-PREP.md` §PWO-039 · `canon/surfaces/visual-language.md` fold ladder

## Goal
Below 138 cols the left gutter sheds and the DECISIONS pane hosts stacked labeled sections — trace first · bare `GOALS` label + digest · bare `FOCUS` label + ranked — height-clip bottom-first (FOCUS sheds before GOALS before trace). `cockpit/fold.py` pure composer reusing three sibling composers verbatim. Empty section = composer called on `None`; all-three-empty collapses to `["—","Exploring…"]`. Per-section hostile containment. Wire gate is exactly `goals is None` inside the existing `decisions is not None` block.

## Scope
- `tw2002_aiclient/cockpit/fold.py` — fold composer
- `tw2002_aiclient/screens.py` — tier gate + fold wire
- `tests/` — 21 Layer-A + five-boundary full-fact ladder sweep + 8 pty

## Constraints
- No h-scroll; fold-active ⊂ decisions-present (structural, verified, no extra poll guard term)
- Fold strictly improves every height ≥22; pre-existing column-independent height split is NOT a 039 regression
- `_FOLD_COMPOSE_FAILED=["—"]` deliberately distinct from real all-empty shape
- Live-resize KEY_RESIZE proof still banked (pty_helpers lacks ctty/SIGWINCH)

## Accept
1. Width tiers: full≥154 · wide-right≥138 · narrow-right≥118 (GOALS+FOCUS fold into DECISIONS) · minimal≥82 · no_border≥60 · refuse<60
2. Below 138, status+suggestions survive inside DECISIONS — do not vanish
3. Layer-A: five-boundary fact ladder (154/138/118/82/60 at/−1)

## Proof
Layer-A — `frame_layout` mode + region x/w at each boundary; fold-section height-clip math.  
Layer-B — pty 40×122 narrow: left gutter gone, DECISIONS shows folded GOALS/FOCUS content; nothing past `max_x-1`.

## Refs
`WO-P3-034-041-panels-PREP.md` §PWO-039 · hub Accept + Push GO (P3-039 in `f594b9e` batch stack)
