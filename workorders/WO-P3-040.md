# WO-P3-040 — Semantic chrome colors (7-tone single source)

> Reconstructed from coord HANDOFF bodies (2026-07-25 backfill).
> Status: **EXECUTED / DONE** 2026-07-24 · tip **`f594b9e`** (CC · Fable 5)
> Type: polish · Phase: 3 · Seat: impl-claudecode-aiclient
> Refs: `WO-P3-034-041-panels-PREP.md` §PWO-040 · `canon/surfaces/visual-language.md` §Cockpit-chrome-tones

## Goal
New pure `cockpit/tones.py` = THE 7-tone single source (read-only `MappingProxyType` — writes raise at the write site; every curses-facing consumer aliases this one object) + both canon classifiers, thresholds verbatim. Launcher's forked `_SEMANTIC_COLORS` table deleted → aliases tones. GAME viewport border = canon STATE surface: cyan chrome → red NON-bold on real `connected: False`; honest-unknown stays cyan. Process-lifetime `_SharedPairs` allocator — one pair per distinct color NAME (order-independent). Mack CRITICAL pair-collision fixed.

## Scope
- `tw2002_aiclient/cockpit/tones.py` — 7-tone table + `status_semantic` + `gauge_semantic` classifiers
- `tw2002_aiclient/screens.py` — re-point launcher + play chrome; viewport border STATE flip
- `tw2002_aiclient/cockpit/draw.py` — `_SharedPairs` process-level allocator
- `tests/` — Layer-A literal canon table pin + boundary classifiers + pty

## Constraints
- `status_semantic` only called with real bool off actual `status` verb (`connected`+`idle_ms`); never claim danger without data
- No `ai_pilot` badge (D5)
- `TW2002_ASCII=1` → color is additive; identical text content
- Game viewport stays server CP437 palette — semantic set is chrome-only

## Accept
1. One 7-tone table (`ok`/`warn`/`danger`/…) drives cockpit chrome
2. GAME viewport border: cyan → red NON-bold on `connected: False`; honest-unknown stays cyan
3. Pair exhaustion degrades without crash; `_SharedPairs` order-independent
4. No `ai_pilot` / `AI-PILOT` badge anywhere

## Proof
Layer-A — tone→attr map + classifier unit tests + boundary-exact thresholds + hostile sweeps.  
Layer-B — pty cell `.fg` / `.bold` for warn vs ok fixtures; dynamic tones-table color assert; ASCII parity; red-AND-non-bold; honest-unknown legs.  
Suite full run exit 0.

## Refs
`WO-P3-034-041-panels-PREP.md` §PWO-040 · `visual-language.md` §Cockpit-chrome-tones · hub Accept + Push GO implicit in `f594b9e` stack
