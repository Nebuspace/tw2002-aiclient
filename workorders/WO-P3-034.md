# WO-P3-034 — GOALS panel live

> Reconstructed from coord HANDOFF bodies (2026-07-25 backfill).
> Status: **EXECUTED / DONE** 2026-07-24 · tip **`6265437`** (CC · Fable 5)
> Type: verify/extend · Phase: 3 · Seat: impl-claudecode-aiclient
> Refs: `WO-P3-034-041-panels-PREP.md` §PWO-034 · `canon/surfaces/trainer-cockpit.md` · `canon/engine/priority-engine.md`

## Goal
Wire `cockpit/goals.py` composer to left-gutter GOALS lines, refreshing from daemon `status` ~1 Hz. Status wire carries no GOALS fields yet — all rows render honest `?` until state_parser/world-model WOs land. Gate: Mack CRITICAL Infinity-crash + 15s-freeze fixed; Pixel clean.

## Scope
- `tw2002_aiclient/cockpit/goals.py` — GOALS composer
- `tw2002_aiclient/screens.py` — stacked left-gutter split + 1 Hz `status_provider` wire
- `tests/` — Layer-A pure compose + Layer-B pty/FakeClient

## Constraints
- No invented priority engine; GOALS is Layer-1 status-only (`priority-engine.md`)
- Archive patterns port by value, never `import twclient`
- `status["goals"]` wire bridge not built — honest-empty until state_parser/world-model ports

## Accept
1. Left-gutter GOALS lines refresh from daemon `status` ~1 Hz with readable labels
2. Empty state honest (`—` / `(none yet)`); never vanishes when unknown
3. Met/partial/unknown/blocked glyphs per canon (`✓`/`·`/`?`/`⊘`)
4. No second priority engine; tips/DECISIONS/coach text read-only display only

## Proof
Layer-A — pure compose from fixture `status` dict → exact lines.  
Layer-B — pty+pyte: `_find_text` for `GOALS` title + known label; FakeClient/status stub; never `run/twd.sock`.

## Refs
`WO-P3-034-041-panels-PREP.md` §PWO-034 · `canon/surfaces/trainer-cockpit.md` N3/GOALS · hub Accept + Push GO @ 12:25:07Z
