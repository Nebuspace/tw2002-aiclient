# WO-P3-031 — Outer border frame (double-line + two-weight hierarchy)

> Reconstructed from coord HANDOFF bodies (2026-07-25 backfill).
> Status: **EXECUTED / DONE** 2026-07-24 · tip **`2a2d65c`** (CC · Fable 5; batch with P3-032 + P3-033)
> Type: build · Phase: 3 · Seat: impl-claudecode-aiclient
> Refs: `WO-P3-030-033-cockpit-frame-PREP.md` §PWO-031 · `canon/surfaces/trainer-cockpit.md` · `canon/surfaces/visual-language.md`

## Goal
Build the double-line outer border frame for PlayShellScreen, establish two-weight box hierarchy (outer double-line vs inner thin-rounded), and enforce the minimum-terminal-size guard.

## Scope
- `tw2002_aiclient/cockpit/layout.py` — `frame_layout(lines, cols)` pure geometry function
- `tw2002_aiclient/cockpit/` draw layer — outer border render
- `tests/test_cockpit_layout.py` (Layer-A)
- `tests/test_cockpit_frame_pty.py` (Layer-B)

## Constraints
- No `import twclient`; archive is reference-only
- `TW2002_ASCII=1` must degrade both box styles to ASCII twins without losing closure
- Do not reintroduce `ai_pilot` / AI-PILOT badge slot (D5)
- Corner/edge `addnstr` inside `try/except curses.error`; shared `_draw_box` per-side try/except

## Accept
1. One double-line outer frame at exactly row 0 / col 0 / `max_y-1` / `max_x-1`, cyan+bold
2. Two-weight hierarchy: outer + game viewport = double-line (`╔═╗║╚╝`); instrument boxes = thin-rounded (`╭─╮│╰╯`); ASCII twin at `TW2002_ASCII=1`
3. No region border overlaps at ≥80×24; below floor → `Terminal too small (C×L) — need at least 60×20`
4. Fold ladder constants from canon (154/138/118/82/60), not stale `>=142` docstring

## Proof
Layer-A — `frame_layout`: corner/edge coords + `mode=="too_small"` below floor.  
Layer-B — pty 40×160: four corner glyphs at corners; `screen.buffer[0][0].fg=="cyan"` and `.bold`; ASCII-twin run; no two region borders share a non-corner cell.

## Refs
`WO-P3-030-033-cockpit-frame-PREP.md` §PWO-031 · visual-language fold floors · ADR-001
