# WO-P3-032 — Character / profile strip (top band row 1)

> Reconstructed from coord HANDOFF bodies (2026-07-25 backfill).
> Status: **EXECUTED / DONE** 2026-07-24 · tip **`2a2d65c`** (CC · Fable 5; batch with P3-031 + P3-033)
> Type: extend · Phase: 3 · Seat: impl-claudecode-aiclient
> Refs: `WO-P3-030-033-cockpit-frame-PREP.md` §PWO-032 · `canon/surfaces/trainer-cockpit.md`

## Goal
Render a `host · game-letter · handle` character/profile strip on row 1 (inside the outer frame), sourced from the reborn `ProfileRow`/`credentials.list_profile_summaries()`. A broken/unresolved profile shows `?`/`—`; strip truncates at line-tail, never wraps.

## Scope
- `tw2002_aiclient/cockpit/` strip composer
- `tw2002_aiclient/screens.py` PlayShellScreen row-1 render
- `tests/` — Layer-A strip-composer unit + Layer-B pty row-1 verify

## Constraints
- Sourced from `ProfileRow` / `credentials.list_profile_summaries()` — **not** archived `world_id_from_profile` (not ported; not needed here)
- No `import twclient`
- Missing fields → `?`/`—` fallback, no crash/blank

## Accept
1. Row 1 renders `<host> · <game-letter> · <handle>` sourced from reborn `ProfileRow`
2. Broken/missing profile shows `?`/`—` for unknown fields; no crash
3. Strip truncates to line-tail at minimal tier, never wraps or h-scrolls

## Proof
Layer-A — unit-test pure strip-composer (fixture profile + broken profile → exact string + `?`/`—` fallbacks).  
Layer-B — pty: `_find_text` for `<host>` + `<handle>` on row 1; broken profile run asserts `?`/`—` + no traceback.

## Refs
`WO-P3-030-033-cockpit-frame-PREP.md` §PWO-032 · `canon/surfaces/trainer-cockpit.md`
