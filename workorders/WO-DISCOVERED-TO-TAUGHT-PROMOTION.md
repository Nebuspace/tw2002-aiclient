# WO-DISCOVERED-TO-TAUGHT-PROMOTION — Discovered cycle → taught macro before arm

**Status:** BANKED · HIGH · Max-gated (canon + ADR) · blocked on design  
**Posted:** 2026-07-28T04:30Z · overnight from #147 CC / hub display-only ruling  
**Refs:** #128 arm-list fence · `autoloop.runner.start(name)` · `ProfitChain` shape

## Goal
A discovered profit cycle becomes armable **only** by becoming a taught macro via a human act
(demonstrate or explicit approve). Never synthesise `name`/`steps[]`/`start_anchor` from the finder.

## Blocker (record)
`ProfitChain` has sectors/hops/margins only — not the three keys `runner.start` needs.
Promotion must produce all three from a human act.

## Accept (when unblocked)
1. Canon + ADR Accepted (Max).
2. Product flow: discovered → teach/approve → named taught row → existing confirm-gate arm.
3. Pins: discovered remains non-armable until promoted; no silent finder→autoloop path.

## Constraints
Money-path. Max-gated. Do not absorb into UI chrome WOs.
