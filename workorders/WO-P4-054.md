# WO-P4-054 — Disconnect viewport chrome (danger border + reconnect)

> Reconstructed from coord HANDOFF bodies (2026-07-25 backfill).
> Status: **EXECUTED / DONE** 2026-07-25 · tip **`6c7d834`** (CC · Opus)
> Type: build/verify · Phase: 4 · Seat: impl-claudecode-aiclient
> Refs: `WO-P4-050-057-viewport-PREP.md` §PWO-054 · `canon/surfaces/visual-language.md` · `canon/surfaces/trainer-cockpit.md`

## Goal
VERIFY reconnect cyan↔danger round-trip + 5-cycle + continuous pty reconnect leg. Silent-border guard via DOCS-WIN `A_UNDERLINE` when danger attr == `A_NORMAL` / color-unavailable, matching `visual-language.md`. Honest-unknown stays cyan. No third border STATE. "Reconnecting" copy still absent / vacuous.

## Scope
- `tw2002_aiclient/screens.py` `PlayShellScreen._viewport_border_attr`
- `tests/test_cockpit_tones_pty.py` — reconnect + exhaustion legs

## Constraints
- Do not invent a second STATE convention
- Height policy / fold unchanged
- "Reconnecting" copy stays outside game cells

## Accept
1. Disconnect → danger border (non-bold / mono underline interim)
2. Reconnect → cyan chrome (round-trip proven; continuous 5-cycle)
3. Honest-unknown stays cyan; no third STATE invented
4. A_UNDERLINE fallback when `danger attr == A_NORMAL` (color-unavailable path)

## Proof
Existing tones/pty pins + dedicated kill-sock or FakeClient `connected: False` leg.  
VERIFY: reconnect round-trip + silent-border guard.

## Refs
`WO-P4-050-057-viewport-PREP.md` §PWO-054 · `visual-language.md` · hub Accept + Push GO @ 01:32:48Z
