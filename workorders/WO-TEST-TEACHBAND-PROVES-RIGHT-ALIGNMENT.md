# WO-TEST-TEACHBAND-PROVES-RIGHT-ALIGNMENT

**Goal:** Pin right-alignment, which the message and canon claim and the assert does not.

**Scope:** `tests/test_cockpit_teachband_pty.py`

**Accept:** A band rendered at column 1 **fails**; the real right-aligned band passes, including its documented yield to other row content.

**Proof:** injected left-shifted band reddens; suite. live-prove `n/a`.

**Refs:** #188 F3 · `test_cockpit_teachband.py:136` both-edges pattern.
