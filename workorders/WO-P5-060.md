# WO-P5-060 — App/Human badge (no AI mode, dual chip)

> Reconstructed from coord HANDOFF bodies (2026-07-25 backfill).
> Status: **EXECUTED / DONE** 2026-07-25 · tip **`2ca3154`** (CC · Fable 5)
> Type: extend · Phase: 5 · Seat: impl-claudecode-aiclient
> Refs: `WO-P5-060-072-mode-teach-PREP.md` §PWO-060 · `canon/surfaces/mode-line-and-teach-controls.md` · ADR-002

## Goal
Control strip shows **App** (green/`ok`+bold+reverse) XOR **Human** (warn/MANUAL+bold+reverse) as the dual; Spectate remains non-member muted label. Strict `is False` App gate; `play.attached` plumbed. Vocabulary AST gate (no `ai_pilot` / `AI-PILOT`). App wire-UNREACHABLE until 061.

## Scope
- `tw2002_aiclient/cockpit/control_seat.py` `APP_LABEL` / `compose_control_strip_segments`
- `tw2002_aiclient/screens.py` / `tw2002_aiclient/cockpit/draw.py` tone wiring
- `tw2002_aiclient/app.py` `play.attached`
- `tests/` — vocabulary gate + label matrix

## Constraints
- Do not unify Spectate into the dual
- Zero `AI-PILOT` / `ai_pilot` UI strings in product paths
- Teach-overlay indicator separate from dual (not this WO)

## Accept
1. Control strip shows `APP` (green/`ok`) XOR `MANUAL — YOU HAVE CONTROL` (warn) as dual
2. SPECTATE remains muted non-member label when not dual-driving
3. Zero `ai_pilot` / `AI-PILOT` UI strings in product paths (rg gate)
4. Strict `is False` App gate (no implicit falsy match)

## Proof
Layer-A composer unit (label selection matrix); `rg` gate no `ai_pilot`/`AI-PILOT` under `tw2002_aiclient/`; optional pty chip text.

## Refs
`WO-P5-060-072-mode-teach-PREP.md` §PWO-060 · hub Accept + Push GO @ 04:42:33Z
