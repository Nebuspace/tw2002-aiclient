# WO-CANON-DRAFT-DEV-DRIVE-EXCEPTION-CITATION-DRIFT

**Goal:** Re-pin `canon/doctrine/dev-drive-exception.md` `session.py` line citations to tip
(substance already correct; pinpoint cites drifted).

**Scope:** `canon/doctrine/dev-drive-exception.md`, `canon/log.md`, this WO.

**Depends-on:** none

**Accept:**
- Code Divergence cites match tip `VALID_SENDERS` / `_require_dev_sender_authorized` /
  `send` / `send_raw` def + gate lines

**Proof:** docs-only; live-prove `n/a`
