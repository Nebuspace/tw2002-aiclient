# WO-P3-037 — HUD freshness markers

> Reconstructed from coord HANDOFF bodies (2026-07-25 backfill).
> Status: **EXECUTED / DONE** 2026-07-24 · tip **`8dda2a6`** (CC · Fable 5)
> Type: build · Phase: 3 · Seat: impl-claudecode-aiclient
> Refs: `WO-P3-034-041-panels-PREP.md` §PWO-037 · `canon/surfaces/trainer-cockpit.md` N4/N8

## Goal
Build `cockpit/hud.py` pure per-tick renderer (5 fields CREDITS/SECTOR/TURNS/CARGO/PROFIT × label/value stride, `format_freshness` `✦ now`/`✦ Ns ago` ASCII `*`, `FRESHNESS_STALE_S=20.0` inclusive boundary, sticky-unknown `-` per canon cold-start — NOT the sibling em-dash; non-finite age pinned unknown-not-stale) and wire off the shared one-poll snapshot, stale→A_DIM. Public `cockpit_draw.draw_lines_attrs` added (per-line attr through identical `_clip_cells`+`_safe_write` choke; `draw_lines` delegates). Poll-guard extended to right_gutter as LATENT hardening only. 83 new tests.

## Scope
- `tw2002_aiclient/cockpit/hud.py` — pure composer
- `tw2002_aiclient/cockpit/draw.py` — `draw_lines_attrs` public API
- `tw2002_aiclient/screens.py` — wire stale→A_DIM
- `tests/test_cockpit_hud.py` (Layer-A) + Layer-B pty

## Constraints
- Ledger is **never** read back as live HUD data (`trainer-cockpit.md` N4/N8)
- `hud_seed` cold-join probe is banked (sibling WO gated on state_parser/tracked-model)
- `status["hud"]` wire bridge not built — all fields honest `-` until then
- draw_lines_attrs must go through `_safe_write` choke (no bypass)

## Accept
1. HUD cells persist last-known values with `✦ Ns ago` / `✦ now` (ASCII `*`)
2. Dim past stale threshold (`FRESHNESS_STALE_S=20.0`)
3. Live values from corrected current screen → tracked model only; ledger never read back as live HUD
4. `draw_lines_attrs` through identical `_clip_cells`+`_safe_write` choke

## Proof
Layer-A — `format_freshness` + compose cells × ages.  
Layer-B — pty cell attrs dim when age ≥ stale; labels CREDITS/SECTOR/TURNS/CARGO/PROFIT order.  
68 Layer-A + 12 pty Layer-B + 3 draw_lines_attrs unit. Full suite 689 exit 0.

## Refs
`WO-P3-034-041-panels-PREP.md` §PWO-037 · hub Accept + Push GO @ 17:30:29Z
