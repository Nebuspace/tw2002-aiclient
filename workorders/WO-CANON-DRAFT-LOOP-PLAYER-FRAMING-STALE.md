# WO-CANON-DRAFT-LOOP-PLAYER-FRAMING-STALE

**Goal:** Remount remaining present-tense `loop_player.py` framing in live canon to tip's
`session/autoloop.py` (citations already correct; body prose lagged).

**Scope:**
- `canon/architecture/app-autopilot-model.md` (AUTO-LOOP driver bullet)
- `canon/architecture/session-engine.md` (Code Divergence actor-tag sentence)
- this WO file

**Out of scope:** archive research tables (historical paths), DECISIONS historical narrative,
code renames, actor-tag rename WO.

**Accept:**
1. Live-canon present-tense AUTO-LOOP driver named as `session/autoloop.py` (or
   `tw2002_aiclient/session/autoloop.py`), not bare `loop_player.py`.
2. Any remaining `loop_player.py` mention is clearly archive/historical.
3. `suite` green; live-prove `n/a` (docs-only).

**Proof:** grep tip `canon/architecture/` for `loop_player.py` → only historical/citation-port
context if any; body bullets name autoloop.
