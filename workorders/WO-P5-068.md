# WO-P5-068 — `T` Assign-trigger scaffold

**Status:** OPEN · READY  
**Posted:** 2026-07-27T05:55:00Z · vision refill — north-star teach wire (Max: larger vision)  
**Seat:** `impl-aiclient-cursor`  
**Depends:** tip ≥ `9028920` · teach strip `T)rigger` label already visible  
**Refs:** `WO-P5-060-072-mode-teach-PREP.md` §PWO-068 · `canon/surfaces/mode-line-and-teach-controls.md` · `canon/engine/` rule-macro (scaffold only)

## Goal

Bind `T` Assign-trigger as a **scaffold**: current screen → macro stub (when+guards schema). Does **not** auto-fire. Does **not** live-drive the game. Full rule engine remains Phase 6.

## Scope

- `T` key intent on Play teach strip → stub binder (schema round-trip)
- Explicit: stub is **not** on the live autopilot fire path
- Tests: unit schema · grep/pin that assign-trigger does not call `explore_start` / send without human gate
- Do **not** steal `T` for explore (explore is `E` + confirm) — teach `T` stays Assign-trigger

## Constraints

- No new CLI verbs.
- No auto-fire on STOP alone.
- Disjoint from CC’s `WO-P5-067` Record paths where possible — coordinate via `🛰️ HEADS-UP` if sharing `screens.py` key dispatch; serialize that file if both touch it.

## Accept

1. `T` creates/updates a stub binding (schema persisted or in-memory with clear Accept path).
2. Stub cannot fire sends without a future approval WO (069/070 family).
3. Explore `E` path unchanged (extreme prove pins still green).
4. PR + STATUS.

## Proof

Unit + existing play-arm / teachband pins green.
