# WO-P3-038 — TX / liveness strip

> Reconstructed from coord HANDOFF bodies (2026-07-25 backfill).
> Status: **EXECUTED / DONE** 2026-07-24 · tip **`7362fc1`** (CC · Fable 5)
> Type: build · Phase: 3 · Seat: impl-claudecode-aiclient
> Refs: `WO-P3-034-041-panels-PREP.md` §PWO-038 · `canon/surfaces/visual-language.md` liveness catalog

## Goal
Add bottom `control_strip` 1-row full-width band with the liveness cluster: heartbeat `●/○` at 0.8s phase from caller-supplied clock; spinner frozen `[0]` honest-calm (no event stream yet); `→ N`/`→ -` TX count-only, values pre-masked upstream. Mode badge/A/R/T left for N5. `cockpit/liveness.py` pure composer with injectable `now_fn` seam for deterministic pty proof. Poll-guard 4th term LIVE at minimal tier. 150 new tests.

## Scope
- `tw2002_aiclient/cockpit/liveness.py` — pure composer
- `tw2002_aiclient/cockpit/layout.py` — 1-row full-width `control_strip` region
- `tw2002_aiclient/screens.py` — wire off shared snapshot
- `tests/` — 132 Layer-A liveness + 9 pty Layer-B + 6 layout boundary

## Constraints
- TX count-only (no secret transits even on upstream failure)
- Spinner frozen `[0]` — honest-calm until event stream exists
- `→` NO-SWAP pinned both glyph sets
- Secrets doctrine: count-only TX holds under planted secrets
- mode badge / A/R/T / operate cluster = Phase 5

## Accept
1. Bottom `control_strip` present at every non-too_small tier
2. Heartbeat `●/○` phase changes per `HEARTBEAT_PERIOD_S=0.8` with injectable clock
3. TX shows count-only `→ N` / idle `→ -`; no secret transits
4. Poll-guard 4th term LIVE at minimal (gutterless tier polls once)

## Proof
Layer-A — phase boundaries both glyph sets; TX falsy-is-idle; hostile sweep.  
Layer-B — two pty captures > period: glyph/phase change; never `run/twd.sock`.  
Suite 838 collected, full run exit 0 ×2 evaluator-side.

## Refs
`WO-P3-034-041-panels-PREP.md` §PWO-038 · `visual-language.md` liveness catalog · hub Accept + Push GO @ 18:28:22Z
