# WO-ENSURE-MATRIX-STAMP-A5CFDDA

**Status:** **SUPERSEDED** by `wo/MATRIX-REFRESH-A5CFDDA` (PR #26) · follow-ons #28–#30 · never built from this seed
**Tip:** `origin/main` after `a5cfdda` (WO-ANET-GAME-SELECT-LETTER-STEP12)

> **Why this seed went unused.** The hub seeded this branch and WO at
> 2026-07-26T19:53Z. By then `impl-claudecode-aiclient` had already created
> `wo/MATRIX-REFRESH-A5CFDDA` and written the artifact against the same tip,
> so the hub retargeted PR #26 at that branch rather than have two seats build
> one matrix stamp. The **work below shipped** — matrix on `main` via #26,
> stamped by #28–#30 — but this file never reached `main`, leaving the WO id
> resolvable only in coord history.
>
> Landed here deliberately rather than deleted with the branch: **a work-order
> id that appears in the coordination record and resolves to nothing is a
> dangling reference**, and chasing two of those cost real time on the day this
> was written. The Goal and Accept below are kept verbatim as the historical
> record of what was asked for.

## Goal

Refresh redacted multi-host ensure matrix audit on current main: rogue · micro · a-net letter **A** (Space Balls; C=closed/`game_closed`). Confirm ≥3 hosts → `main_command`.

## Do

1. Update `audit/live-ensure-matrix-*.md` (redacted) citing tip SHA + per-cell class@step / PASS.
2. Host order: rogue NEW+RETURNING regression first → micro → a-net A last (cool-off).
3. Record honest `game_closed` if anyone re-proves letter C (optional one-liner).
4. Hub runs live cells if seat Shell dead; seat prepares checklist/audit skeleton.

## Out

Explore (M4 HOLD) · invent · Autopilot · credential path changes

## Accept

Redacted matrix on tip with ≥3 PASS cells to `main_command` (or hub live-prove cited). STATUS with SHA.
