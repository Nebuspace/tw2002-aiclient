# WO-CANON-DRAFT-COACH-PROVENANCE-CLI-CATALOG

**Goal:** Catalog tip-true `tw coach provenance` in `canon/architecture/cli-verbs.md`
(wired since #685 / `coach_cli.py`; table previously listed only `coach show`).

**Scope:** `canon/architecture/cli-verbs.md`, `canon/log.md`, this WO.

**Depends-on:** none (CLI already shipped)

**Accept:**
- Session-primitives / read-only table row for `coach provenance` with key args
- LIVE enumeration reflects `coach {show,provenance}`
- Owning concepts: Coverage Metrics (+ Coaching Engine)

**Proof:** docs-only; live-prove `n/a`
