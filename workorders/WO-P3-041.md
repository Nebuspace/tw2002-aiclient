# WO-P3-041 — LOGS band (redacted transcript tail)

> Reconstructed from coord HANDOFF bodies (2026-07-25 backfill).
> Status: **EXECUTED / DONE** 2026-07-24 · tip **`6391bb7`** (CC · Fable 5) · Phase 3 CLOSED
> Type: extend · Phase: 3 · Seat: impl-claudecode-aiclient
> Refs: `WO-P3-034-041-panels-PREP.md` §PWO-041 · `canon/surfaces/trainer-cockpit.md` N7 · `canon/architecture/secrets-and-credentials.md`

## Goal
Session-side `TranscriptTail` ring (`TAIL_MAX=100`, redact-at-insert, `append_redacted` structurally payload-proof, marker `<<secret input redacted>>` mirroring `logging_util` wording) served as additive `status["log_tail"]`. Pure `cockpit.logsband` composer (newest-last, oldest-dropped-first clip, honest-empty `(none yet)`, full hostile-hardening family); screens wire with `status_line` fallback keyed off `has_real_tail`; newest-row flash `TICKER_FLASH_DURATION_S=1.0` per canon. Phase 3 CLOSED.

## Scope
- `tw2002_aiclient/session/transcript_tail.py` — TranscriptTail ring (session side)
- `tw2002_aiclient/cockpit/logsband.py` — pure composer
- `tw2002_aiclient/screens.py` — wire + flash
- `tests/test_transcript_tail.py` (Layer-A) + `tests/test_cockpit_logsband.py` + `tests/test_cockpit_logsband_pty.py`

## Constraints
- Redaction applied AT INSERT — raw secret never enters the buffer
- Fixture secrets: obviously-fake sentinels only; every secret-path assert = marker PRESENT + sentinel ABSENT
- Poll guard: logs unconditional at every non-too_small tier; no new poll-guard term

## Accept
1. Full-width `[LOGS]` transcript tail; lines appear as session advances
2. **Redacted** — passwords never shown; newest row flash OK per canon
3. `LOGS` present at every non-too_small tier incl. minimal/no_border
4. Mack adversarial pass: whole-grid sentinel sweep + ESC/CSI containment + concurrency

## Proof
Layer-A — redaction unit + tail compose (LOGS + non-secret line; secret fixture → marker present + sentinel absent).  
Layer-B — pty `_find_text` for `LOGS` + advancing non-secret line; secret fixture asserts redaction marker grid-wide.

## Refs
`WO-P3-034-041-panels-PREP.md` §PWO-041 · hub Accept `6391bb7` · Phase 3 CLOSED
