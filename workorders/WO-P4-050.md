# WO-P4-050 — Watch-stream subscribe (product WatchFeed client)

> Reconstructed from coord HANDOFF bodies (2026-07-25 backfill).
> Status: **EXECUTED / DONE** 2026-07-24 · tip **`b211be2`** (CC · Fable 5)
> Type: build · Phase: 4 · Seat: impl-claudecode-aiclient
> Refs: `WO-P4-050-057-viewport-PREP.md` §PWO-050 · `canon/surfaces/trainer-cockpit.md`

## Goal
Product `WatchFeed` lifetime-subscribe client (`watchfeed.py`) — protocol-shaped, structural no-send API. Latest-wins `WatchFeedSnapshot` (`events_received`/`latest_event`/`running`). Per-line containment (malformed/non-dict JSON dropped, reader never raises). `shutdown(SHUT_RDWR)`-before-`close()` unblock giving a bounded (~2s join) idempotent `stop()`. `app.py` wire scoped to the play binding only — feed started before frame loop, released in `try`/`finally`. AST tripwire allowlist: single-site `_is_allowlisted_watchfeed_stop` preserves guard behavioral intent.

## Scope
- `tw2002_aiclient/watchfeed.py` — `WatchFeed` client
- `tw2002_aiclient/app.py` — play-binding wire
- `tests/test_watchfeed.py` (Layer-A) · `tests/test_watchfeed_wire.py` (wiring)

## Constraints
- **No game sends** on the subscribe channel; subscribe only
- Do not conflate with `tw watch` CLI (ops, different consumer)
- Do not take control_lock
- One redraw owner (no TOCTOU with existing status-poll redraw)

## Accept
1. Play shell opens a lifetime `subscribe` connection; settle-edge events arrive
2. No `do`/`send` from subscribe path (no game sends)
3. Disconnect/unsubscribe clean on Esc/back; `stop()` bounded and idempotent

## Proof
Layer-A — FakeClient / fake hub pushes N settle events → adapter records N.  
Assert no `do`/`send` from subscribe path.

## Refs
`WO-P4-050-057-viewport-PREP.md` §PWO-050 · WatchHub live (P2) · hub Accept + Push GO @ 22:31:22Z
