# WO-P4-051 — Centre 80×25 viewport shell (placeholder removed)

> Reconstructed from coord HANDOFF bodies (2026-07-25 backfill).
> Status: **EXECUTED / DONE** 2026-07-24 · tip **`7af4dbd`** · (CC · Opus, then grew to 80×25 in `bb780e0`)
> Type: build · Phase: 4 · Seat: impl-claudecode-aiclient
> Refs: `WO-P4-050-057-viewport-PREP.md` §PWO-051 · `canon/surfaces/trainer-cockpit.md`

## Goal
Remove `_GAME_PLACEHOLDER` from `PlayShellScreen.draw()` centre block. Bordered tiers draw double-line border + `GAME` title only (zero interior content draw — blankness is `stdscr.erase()`'s own leftover). No-border tier draws nothing into `center`. Layer-A pins exact 80×25 interior budget at every bordered tier; Layer-B pty asserts placeholder-absent grid-wide + positive blank-interior scan. History: initially 80×24; grew to **80×25** / bordered **82×27** in `bb780e0` (Accept/Proof carry current contract).

## Scope
- `tw2002_aiclient/screens.py` (`PlayShellScreen.draw()` centre block)
- `tw2002_aiclient/cockpit/layout.py` — viewport constants `VIEWPORT_W/H`, `GAME_W/H`
- `tests/test_cockpit_layout.py` (Layer-A) · `tests/test_cockpit_viewport.py` (Layer-A) · `tests/test_cockpit_frame_pty.py` + `tests/test_cockpit_viewport_pty.py` (Layer-B)

## Constraints
- Do not paint real pyte yet (052)
- Do not break Esc→launcher / ADR-001 one-tree
- Placeholder `_GAME_PLACEHOLDER` string must not reach any `addstr` call in full draw pass

## Accept
1. Bordered `GAME` box shows empty 80×25 interior (zero interior content draw); double-line border per visual-language
2. Geometry matches `VIEWPORT_W/H` / `GAME_W/H` constants
3. Placeholder string gone (grid-wide assert + no `addstr` hit)

## Proof
Layer-A — layout regions + content cell budget 80×25; pure fake-stdscr recorder.  
Layer-B — pty: no placeholder text; title `GAME` present; narrow/no_border tiers sane.

## Refs
`WO-P4-050-057-viewport-PREP.md` §PWO-051 · hub Accept `7af4dbd` + 80×25 grow `bb780e0`
