# WO-P5-063 — Confirm-to-arm dialog

> Status: **READY** · WO-P5-066 on main (`01b94e7`) · CC+Fable
> Type: build · Phase: 5 · Seat: impl-claudecode-aiclient (planned)
> Refs: `WO-P5-060-072-mode-teach-PREP.md` §PWO-063 · `canon/surfaces/mode-line-and-teach-controls.md` · `canon/architecture/app-autopilot-model.md`

## Goal
Arm the autopilot/trainer requires an explicit confirm step — no silent arm on bare Enter. A danger+reverse dialog gate (loudest mode-line palette combo) guards arm, matching the money/turns-risk visual language in canon.

## Scope
- `tw2002_aiclient/cockpit/` — confirm-to-arm dialog composer
- `tw2002_aiclient/screens.py` — dialog wire / key dispatch
- `tests/` — Layer-A dialog compose + key matrix (y/N paths)

## Constraints
- Bare Enter must **not** fire arm — explicit `y` required
- Use danger+reverse (loudest palette combo) per visual-language canon; do not invent a new palette
- Arm write-back is still read-only (062 stub); this WO only adds the confirm gate, not the daemon call
- Depends-on: **062 DONE** (arm indicator on origin); **serialize after 066 Accept** (hub ruling)
- Do not touch protocol/daemon; no silent arm inject

## Accept
1. Arm requires explicit confirm (`y`); `N` / Esc / any other key cancels with no state change
2. Confirm dialog rendered with danger+reverse palette (matches mode-line canon)
3. Bare Enter does **not** arm
4. Layer-A dialog compose test covers y/N key matrix
5. No silent arm inject in any code path

## Proof
- Layer-A composer unit: dialog present, correct palette attribute, y→arm-intent, N/Enter→cancel
- Key matrix table: y · N · Enter · Esc — expected outcomes
- `rg` no silent arm call outside explicit-confirm path

## Depends-on
- **WO-P5-062** DONE (`b712637`) — arm indicator on origin
- **WO-P5-066** Accept on origin (hub serializes; do not execute until hub posts GO)

## Refs
`WO-P5-060-072-mode-teach-PREP.md` §PWO-063 · serialize ruling: after 066 Accept (hub) · visual-language canon: danger+reverse = loudest palette, reserved for money/turns risk
